export type ChildrenProps = { children: React.ReactChild | React.ReactChild[] };

export type CardProps = { imgUrl: string; title: string; votes: string };

export type BtnProps = {
  txtBtn: string;
  onClick: any;
  isActive: boolean;
};
